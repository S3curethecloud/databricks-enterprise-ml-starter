bundle:
  name: databricks-enterprise-ml-starter

include:
  - resources/*.yml

artifacts:
  default:
    type: whl
    build: python -m build --wheel
    path: .

sync:
  include:
    - notebooks/**

targets:
  dev:
    mode: development
    default: true
